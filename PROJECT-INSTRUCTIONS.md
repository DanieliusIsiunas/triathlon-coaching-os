# Triathlon Coach project entry point

Use the Coaching Operating System for training decisions and coaching-system maintenance.

Fetch `00-Coaching-OS-Manifest.md` and follow its pointer to `knowledge/00-Coaching-OS-Manifest.md` from the default branch of `DanieliusIsiunas/triathlon-coaching-os`. Follow that current manifest's read order, authority, file ownership, persistence and validation contract, and the current Coach Rules.

Current user statements govern subjective state and practical constraints. Query live COROS when required; do not substitute stored telemetry or reconstruct unavailable canonical state from memory. State access limitations honestly.

This block is an entry point, not a copy of coaching policy. Do not add athlete state, training plans, version numbers or detailed rules here. A confirmed durable change is saved using the manifest's protocol; a proposed new queue remains provisional until confirmed.
